using UnityEngine;

namespace Script.Enum
{
    public enum Difficulty
    {
        Easy,
        Normal,
        Hard,
    }
    
    public enum DungeonEnv
    {
        Plane,
        Volcano,
        Swamp,
    }
    
    public enum Size
    {
        Village,
        Domain,
        Nation,
        Continent,
        World
    }
    
    public enum DungeonType
    {
        
    }
}
